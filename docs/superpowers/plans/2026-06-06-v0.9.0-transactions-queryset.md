# v0.9.0 — Transactions ORM (QuerySet) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Étendre les transactions au QuerySet et au bulk, et permettre les lectures dans une transaction, via un modèle hybride (buffering HTTP conservé du v0.8.0 + transaction WebSocket interactive sur connexion dédiée).

**Architecture:** `transaction.py` expose une base `Transaction` (machinerie post-commit + `raise_for_status`, conservées) et deux stratégies : `BufferedTransaction` (HTTP, = v0.8.0) et `InteractiveTransaction` (WS, nouvelle, connexion dédiée injectée). `connection_manager.transaction()` choisit la stratégie selon le schéma de l'URL. Les call sites (`model_base`, `query_set`) utilisent une interface unique `await tx.add(sql, vars)` (écriture) / `await tx.run_read(sql, vars)` (lecture).

**Tech Stack:** Python 3.11+, Pydantic v2, `surrealdb[pydantic]>=2.0.0,<3.0.0`, pytest + pytest-asyncio, ruff, mypy, SurrealDB v2.6.0 / v3.1.3 (Docker).

---

## Prérequis (environnement de test)

Les tests E2E exigent un SurrealDB local. Avant d'exécuter les tâches E2E :

```bash
docker run -d --name surreal -p 8000:8000 surrealdb/surrealdb:v2.6.0 start --user root --pass root
curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/health   # attendre 200
```

Lancer un test : `SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest <chemin> -v`

Nettoyage : `docker rm -f surreal`. Avant push, rejouer aussi sur `surrealdb/surrealdb:v3.1.3`.

Conventions du repo : après chaque tâche touchant `src/`, lancer `uv run ruff format src/` et `uv run ruff check src/`. Inclure `uv.lock` si modifié.

---

## File Structure

- `src/surreal_orm_lite/transaction.py` — **réécrit** : base `Transaction` (ABC), `BufferedTransaction`, `InteractiveTransaction`.
- `src/surreal_orm_lite/connection_manager.py` — **modifié** : `_open_client()` extrait, `transaction()` recâblé (dispatch HTTP/WS).
- `src/surreal_orm_lite/model_base.py` — **modifié** : `objects(tx=)`, branches `tx=` via `await tx.add()`, `refresh(tx=)` WS, `merge(tx=)` refresh WS, auto-id save WS.
- `src/surreal_orm_lite/query_set.py` — **modifié** : `self._tx`, routage lectures via `run_read`, bulk via `add`.
- `src/surreal_orm_lite/__init__.py` — **modifié** : version `0.9.0`, exports des sous-classes.
- `pyproject.toml` — **modifié** : `version = "0.9.0"`.
- `tests/test_transactions.py` — **modifié** : unitaires ré-aiguillés vers `BufferedTransaction`, ajouts `InteractiveTransaction` (unit + E2E QuerySet/HTTP/WS).
- `docs/ROADMAP.md` — **modifié** : v0.9.0 ✅ + note savepoints.

---

## Task 1: Spike E2E — valider la transaction WS interactive du SDK officiel

**Files:**
- Test: `tests/test_transactions.py` (ajout en fin de fichier)

- [ ] **Step 1: Écrire le test E2E spike**

Ajouter à la fin de `tests/test_transactions.py` (réutilise `_connect()` déjà présent) :

```python
class TestInteractiveTxSpikeE2E:
    """Valide l'hypothèse fondatrice : sur une connexion WS, l'état de transaction
    persiste entre des appels query() successifs (BEGIN ... ops ... COMMIT/CANCEL)."""

    @pytest.mark.asyncio
    async def test_ws_transaction_state_persists_across_queries(self) -> None:
        _connect()  # ws://...
        client = await SurrealDBConnectionManager.get_client()
        with contextlib.suppress(Exception):
            await client.query("DELETE TxSpike;", {})

        await client.query("BEGIN TRANSACTION;", {})
        await client.query("CREATE TxSpike:s1 CONTENT { name: 'x' };", {})
        # La lecture dans la même transaction DOIT voir l'écriture non-committée.
        seen = await client.query("SELECT * FROM TxSpike;", {})
        assert isinstance(seen, list) and len(seen) == 1
        await client.query("CANCEL TRANSACTION;", {})

        # Après CANCEL, l'écriture a disparu (rollback effectif).
        after = await client.query("SELECT * FROM TxSpike;", {})
        assert after == [] or len(after) == 0
        await SurrealDBConnectionManager.close_connection()
```

- [ ] **Step 2: Lancer le test (serveur Docker requis)**

Run: `SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest tests/test_transactions.py::TestInteractiveTxSpikeE2E -v`
Expected: PASS. **Si ÉCHEC** (l'état de tx ne persiste pas entre appels) : STOP, rouvrir le design (se rabattre sur buffering pur WS + lectures non supportées).

- [ ] **Step 3: Commit**

```bash
git add tests/test_transactions.py
git commit -m "test(tx): spike validating WS interactive transactions across query() calls"
```

---

## Task 2: Extraire `_open_client()` dans le connection manager

**Files:**
- Modify: `src/surreal_orm_lite/connection_manager.py` (méthodes `get_client`, ajout `_open_client`)
- Test: `tests/test_transactions.py` (ajout)

- [ ] **Step 1: Écrire le test E2E (connexion dédiée distincte du singleton)**

Ajouter à `tests/test_transactions.py` :

```python
class TestOpenClientE2E:
    @pytest.mark.asyncio
    async def test_open_client_returns_fresh_connection(self) -> None:
        _connect()
        shared = await SurrealDBConnectionManager.get_client()
        dedicated = await SurrealDBConnectionManager._open_client()
        try:
            assert dedicated is not shared  # nouvelle connexion, non mise en cache
            # la dédiée est fonctionnelle
            res = await dedicated.query("RETURN 1;", {})
            assert res == 1 or res == [1] or (isinstance(res, list) and res[0] == 1)
        finally:
            with contextlib.suppress(Exception):
                await dedicated.close()
            await SurrealDBConnectionManager.close_connection()
```

- [ ] **Step 2: Lancer le test, vérifier l'échec**

Run: `SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest tests/test_transactions.py::TestOpenClientE2E -v`
Expected: FAIL avec `AttributeError: ... has no attribute '_open_client'`

- [ ] **Step 3: Implémenter `_open_client()` et refactorer `get_client()`**

Dans `connection_manager.py`, remplacer la méthode `get_client` (lignes ~60-101) par :

```python
    @classmethod
    async def _open_client(cls) -> Any:
        """Open a NEW authenticated connection (never cached).

        Used both for the shared singleton (via get_client) and for the dedicated
        connection of an interactive WS transaction.
        """
        if not cls.is_connection_set():
            raise ValueError("Connection not been set.")
        assert cls.__url is not None
        assert cls.__namespace is not None
        assert cls.__database is not None
        assert cls.__user is not None
        assert cls.__password is not None

        url = cls.__url
        _client = AsyncSurreal(url)
        if url.startswith(("ws://", "wss://")):
            await _client.connect(url)
        await _client.signin({"username": cls.__user, "password": cls.__password})
        await _client.use(cls.__namespace, cls.__database)
        return _client

    @classmethod
    async def get_client(cls) -> Any:
        """
        Connect to the SurrealDB instance (cached singleton).

        :return: The SurrealDB instance.
        """
        if cls.__client is not None:
            return cls.__client

        try:
            cls.__client = await cls._open_client()
            return cls.__client
        except ValueError:
            raise
        except Exception as e:
            logger.warning(f"Can't get connection: {e}")
            cls.__client = None
            raise SurrealDbConnectionError("Can't connect to the database.") from None
```

- [ ] **Step 4: Lancer le test, vérifier le succès + non-régression**

Run: `SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest tests/test_transactions.py::TestOpenClientE2E tests/test_manager.py -v`
Expected: PASS

- [ ] **Step 5: Lint + commit**

```bash
uv run ruff format src/ && uv run ruff check src/
git add src/surreal_orm_lite/connection_manager.py tests/test_transactions.py
git commit -m "refactor(connection): extract _open_client() for dedicated tx connections"
```

---

## Task 3: Réécrire `transaction.py` — base `Transaction` + `BufferedTransaction`

**Files:**
- Modify: `src/surreal_orm_lite/transaction.py` (réécriture)
- Test: `tests/test_transactions.py` (ré-aiguiller les unitaires existants)

- [ ] **Step 1: Mettre à jour les tests unitaires existants vers `BufferedTransaction`**

Dans `tests/test_transactions.py` :

1. Modifier l'import :

```python
from surreal_orm_lite.transaction import BufferedTransaction, InteractiveTransaction, Transaction
```

2. Remplacer, dans les tests unitaires suivants, `Transaction()` par `BufferedTransaction()` :
   `test_add_namespaces_variables`, `test_build_query_wraps_in_transaction`,
   `test_build_query_empty_raises`, `test_is_empty`, `test_add_namespacing_respects_word_boundary`.
   Et rendre `add` awaité (la méthode devient async). Exemple pour `test_add_namespaces_variables` :

```python
@pytest.mark.asyncio
async def test_add_namespaces_variables() -> None:
    tx = BufferedTransaction()
    await tx.add("CREATE User:a CONTENT $data;", {"data": {"n": 1}})
    await tx.add("UPDATE User:b MERGE $data;", {"data": {"n": 2}})
    assert tx.variables == {"t0_data": {"n": 1}, "t1_data": {"n": 2}}
    assert tx.statements == [
        "CREATE User:a CONTENT $t0_data;",
        "UPDATE User:b MERGE $t1_data;",
    ]
```

Faire de même (ajouter `@pytest.mark.asyncio` + `async` + `await tx.add(...)`) pour
`test_build_query_wraps_in_transaction`, `test_is_empty`, `test_add_namespacing_respects_word_boundary`.
`test_build_query_empty_raises` reste sync mais utilise `BufferedTransaction()`.
`test_raise_for_status_*` restent inchangés (la méthode statique reste sur `Transaction`).
`test_transaction_is_exported` reste inchangé.

3. Ajouter un test du contrat de lecture buffering :

```python
@pytest.mark.asyncio
async def test_buffered_run_read_raises() -> None:
    tx = BufferedTransaction()
    assert tx.is_interactive is False
    with pytest.raises(SurrealDbError, match="WebSocket"):
        await tx.run_read("SELECT * FROM User;", {})
```

- [ ] **Step 2: Lancer les tests unitaires, vérifier l'échec**

Run: `uv run pytest tests/test_transactions.py -k "add_namespaces or build_query or is_empty or word_boundary or buffered_run_read" -v`
Expected: FAIL (`ImportError: cannot import name 'BufferedTransaction'`)

- [ ] **Step 3: Réécrire `transaction.py` (base + BufferedTransaction)**

Remplacer le contenu de `src/surreal_orm_lite/transaction.py` par :

```python
# src/surreal_orm_lite/transaction.py
"""ORM-level transactions for the official SurrealDB SDK 2.0.

Two strategies share one public interface:

- ``BufferedTransaction`` (HTTP): buffers each operation's SurrealQL statement and
  flushes them as a single ``BEGIN … COMMIT`` query at commit. Reads inside the
  transaction are impossible (HTTP is stateless) and raise. This is the v0.8.0 model.
- ``InteractiveTransaction`` (WebSocket): runs each operation immediately on a
  dedicated connection inside a server-side ``BEGIN … COMMIT`` block, so reads see
  uncommitted writes.

``connection_manager.transaction()`` picks the strategy based on the configured URL.
"""

import re
from abc import ABC, abstractmethod
from collections.abc import Awaitable, Callable
from typing import Any

from .exceptions import SurrealDbError


class Transaction(ABC):
    """Base transaction: shared post-commit machinery + the call-site interface.

    Call sites use ``await tx.add(sql, vars)`` for writes and
    ``await tx.run_read(sql, vars)`` for reads, and never care which concrete
    strategy is in use. ``is_interactive`` lets the few read-dependent paths
    (auto-id save, merge refresh, refresh) adapt.
    """

    def __init__(self) -> None:
        self._post_commit: list[Callable[[], Awaitable[None]]] = []

    @property
    @abstractmethod
    def is_interactive(self) -> bool:
        """True for the WS interactive strategy (reads supported)."""
        ...

    @abstractmethod
    async def begin(self) -> None: ...

    @abstractmethod
    async def add(self, statement: str, variables: dict[str, Any] | None = None) -> Any:
        """Register a write. Buffered: queue (returns None). Interactive: run now
        (returns the statement's result rows)."""
        ...

    @abstractmethod
    async def run_read(self, statement: str, variables: dict[str, Any] | None = None) -> Any:
        """Run a read and return rows. Buffered: raises (reads need WebSocket)."""
        ...

    @abstractmethod
    async def commit(self) -> None: ...

    @abstractmethod
    async def cancel(self) -> None: ...

    def enqueue_post_commit(self, callback: Callable[[], Awaitable[None]]) -> None:
        """Enqueue an async callback to run after a successful commit (deferred signals)."""
        self._post_commit.append(callback)

    async def fire_post_commit(self) -> None:
        """Invoke every enqueued post-commit callback in insertion order (FIFO).

        The first callback that raises propagates; the rest are NOT invoked. A raised
        exception does NOT undo the commit (the write is already durable).
        """
        for cb in self._post_commit:
            await cb()

    @staticmethod
    def raise_for_status(raw: Any) -> None:
        """Inspect a ``query_raw()`` response and raise if any statement failed.

        ``query()`` silently returns ``None`` on a failed transaction, so the
        commit/write paths use ``query_raw()`` and call this. Any statement with
        ``status == "ERR"`` means the transaction rolled back. Prefers the root cause
        (the first ERR whose ``details.kind`` is not ``"NotExecuted"``).
        """
        if not isinstance(raw, dict) or not isinstance(raw.get("result"), list):
            raise SurrealDbError(f"Transaction failed: unrecognized query_raw() response shape: {raw!r}")
        statements = raw["result"]
        errors = [s for s in statements if isinstance(s, dict) and s.get("status") == "ERR"]
        if not errors:
            return
        root = next(
            (e for e in errors if (e.get("details") or {}).get("kind") != "NotExecuted"),
            errors[0],
        )
        message = root.get("result") or "transaction failed"
        raise SurrealDbError(f"Transaction failed and rolled back: {message}")


class BufferedTransaction(Transaction):
    """HTTP strategy (v0.8.0): buffer statements, flush as one BEGIN…COMMIT query.

    Requires a shared client at commit time, injected by the connection manager.
    Record ids inlined into statements are the caller's responsibility (see save()).
    """

    def __init__(self, client: Any = None) -> None:
        super().__init__()
        self._client = client
        self.statements: list[str] = []
        self.variables: dict[str, Any] = {}
        self._counter: int = 0

    @property
    def is_interactive(self) -> bool:
        return False

    @property
    def is_empty(self) -> bool:
        return not self.statements

    async def begin(self) -> None:
        # HTTP is stateless: nothing to send until commit.
        return None

    async def add(self, statement: str, variables: dict[str, Any] | None = None) -> None:
        """Append a statement, renaming its ``$vars`` with a per-statement prefix.

        Each ``$name`` becomes ``$t<N>_name`` (matched at a word boundary so a name
        that is a prefix of another is not corrupted). Returns None: nothing executes
        until commit, so buffered writes cannot return rows.
        """
        prefix = f"t{self._counter}_"
        renamed = statement
        if variables:
            for name, value in variables.items():
                renamed = re.sub(rf"\${re.escape(name)}\b", f"${prefix}{name}", renamed)
                self.variables[f"{prefix}{name}"] = value
        self.statements.append(renamed)
        self._counter += 1
        return None

    async def run_read(self, statement: str, variables: dict[str, Any] | None = None) -> Any:
        raise SurrealDbError(
            "Reads inside a transaction require a WebSocket (interactive) transaction; "
            "the current connection is HTTP (buffered)."
        )

    def build_query(self) -> str:
        """Return the batched ``BEGIN … COMMIT`` query string."""
        if self.is_empty:
            raise ValueError("Cannot commit an empty transaction.")
        body = "\n".join(self.statements)
        return f"BEGIN TRANSACTION;\n{body}\nCOMMIT TRANSACTION;"

    async def commit(self) -> None:
        if self.is_empty:
            return
        # query_raw (NOT query): query() returns None on a failed tx; query_raw gives
        # per-statement status for raise_for_status.
        raw = await self._client.query_raw(self.build_query(), self.variables)
        self.raise_for_status(raw)

    async def cancel(self) -> None:
        # Nothing was sent (statements buffered) → rollback is free.
        self.statements = []
        self.variables = {}
```

- [ ] **Step 4: Lancer les tests unitaires, vérifier le succès**

Run: `uv run pytest tests/test_transactions.py -k "add_namespaces or build_query or is_empty or word_boundary or buffered_run_read or raise_for_status or is_exported" -v`
Expected: PASS

- [ ] **Step 5: Lint + commit**

```bash
uv run ruff format src/ && uv run ruff check src/
git add src/surreal_orm_lite/transaction.py tests/test_transactions.py
git commit -m "refactor(tx): split Transaction into base + BufferedTransaction (HTTP)"
```

---

## Task 4: Implémenter `InteractiveTransaction` (WS)

**Files:**
- Modify: `src/surreal_orm_lite/transaction.py` (ajout de la classe)
- Test: `tests/test_transactions.py` (unitaires avec faux client)

- [ ] **Step 1: Écrire les tests unitaires (faux client, sans serveur)**

Ajouter à `tests/test_transactions.py` :

```python
class _FakeClient:
    """Async client double recording calls; returns OK envelopes by default."""

    def __init__(self) -> None:
        self.calls: list[tuple[str, str]] = []
        self.read_rows: list[Any] = [{"id": "User:1", "name": "x"}]
        self.write_envelope: dict[str, Any] = {"result": [{"status": "OK", "result": [{"id": "User:1"}]}]}
        self.err_envelope: dict[str, Any] | None = None

    async def query_raw(self, sql: str, vars: dict[str, Any] | None = None) -> dict[str, Any]:
        self.calls.append(("query_raw", sql))
        if self.err_envelope is not None and "CREATE" in sql:
            return self.err_envelope
        return self.write_envelope

    async def query(self, sql: str, vars: dict[str, Any] | None = None) -> Any:
        self.calls.append(("query", sql))
        return self.read_rows

    async def close(self) -> None:
        self.calls.append(("close", ""))


@pytest.mark.asyncio
async def test_interactive_begin_sends_begin() -> None:
    c = _FakeClient()
    tx = InteractiveTransaction(c)
    assert tx.is_interactive is True
    await tx.begin()
    assert ("query_raw", "BEGIN TRANSACTION;") in c.calls


@pytest.mark.asyncio
async def test_interactive_add_runs_and_returns_rows() -> None:
    c = _FakeClient()
    tx = InteractiveTransaction(c)
    rows = await tx.add("CREATE User:1 CONTENT $data;", {"data": {"name": "x"}})
    assert rows == [{"id": "User:1"}]
    assert any(kind == "query_raw" and "CREATE" in sql for kind, sql in c.calls)


@pytest.mark.asyncio
async def test_interactive_add_raises_on_err() -> None:
    c = _FakeClient()
    c.err_envelope = {"result": [{"status": "ERR", "result": "boom"}]}
    tx = InteractiveTransaction(c)
    with pytest.raises(SurrealDbError, match="rolled back"):
        await tx.add("CREATE User:1 CONTENT $data;", {"data": {}})


@pytest.mark.asyncio
async def test_interactive_run_read_returns_rows() -> None:
    c = _FakeClient()
    tx = InteractiveTransaction(c)
    rows = await tx.run_read("SELECT * FROM User;", {})
    assert rows == [{"id": "User:1", "name": "x"}]
    assert ("query", "SELECT * FROM User;") in c.calls


@pytest.mark.asyncio
async def test_interactive_commit_and_cancel() -> None:
    c = _FakeClient()
    tx = InteractiveTransaction(c)
    await tx.commit()
    assert ("query_raw", "COMMIT TRANSACTION;") in c.calls
    c2 = _FakeClient()
    tx2 = InteractiveTransaction(c2)
    await tx2.cancel()
    assert ("query", "CANCEL TRANSACTION;") in c2.calls
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `uv run pytest tests/test_transactions.py -k interactive -v`
Expected: FAIL (`ImportError: cannot import name 'InteractiveTransaction'`)

- [ ] **Step 3: Implémenter `InteractiveTransaction`**

Ajouter à la fin de `src/surreal_orm_lite/transaction.py` :

```python
class InteractiveTransaction(Transaction):
    """WebSocket strategy: run each operation immediately inside a server-side
    BEGIN…COMMIT on a dedicated connection (injected by the connection manager).

    Reads see uncommitted writes. The connection manager owns the connection
    lifecycle (it closes the dedicated client after commit/cancel).
    """

    def __init__(self, client: Any) -> None:
        super().__init__()
        self._client = client

    @property
    def is_interactive(self) -> bool:
        return True

    async def begin(self) -> None:
        await self._client.query_raw("BEGIN TRANSACTION;")

    async def add(self, statement: str, variables: dict[str, Any] | None = None) -> Any:
        """Run a write immediately; inspect status; return the statement's rows."""
        raw = await self._client.query_raw(statement, variables or {})
        self.raise_for_status(raw)
        statements = raw["result"]
        return statements[-1]["result"] if statements else []

    async def run_read(self, statement: str, variables: dict[str, Any] | None = None) -> Any:
        """Run a read immediately and return rows directly (matches non-tx query())."""
        return await self._client.query(statement, variables or {})

    async def commit(self) -> None:
        raw = await self._client.query_raw("COMMIT TRANSACTION;")
        self.raise_for_status(raw)

    async def cancel(self) -> None:
        import contextlib

        with contextlib.suppress(Exception):
            await self._client.query("CANCEL TRANSACTION;")
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `uv run pytest tests/test_transactions.py -k interactive -v`
Expected: PASS

- [ ] **Step 5: Lint + commit**

```bash
uv run ruff format src/ && uv run ruff check src/
git add src/surreal_orm_lite/transaction.py tests/test_transactions.py
git commit -m "feat(tx): add InteractiveTransaction (WS, dedicated connection)"
```

---

## Task 5: Recâbler `connection_manager.transaction()` (dispatch HTTP/WS)

**Files:**
- Modify: `src/surreal_orm_lite/connection_manager.py` (import + `transaction()`)
- Test: `tests/test_transactions.py` (E2E WS + HTTP)

- [ ] **Step 1: Écrire les tests E2E (WS interactif + HTTP buffering)**

Ajouter à `tests/test_transactions.py` :

```python
def _connect_http() -> None:
    SurrealDBConnectionManager.set_connection(
        url=f"http://{os.environ.get('SURREALDB_HOST', 'localhost')}:{os.environ.get('SURREALDB_PORT', '8000')}",
        user="root",
        password="root",
        namespace="ns",
        database="db",
    )


class TestTransactionDispatchE2E:
    @pytest.mark.asyncio
    async def test_ws_uses_interactive(self) -> None:
        _connect()  # ws://
        async with SurrealDBConnectionManager.transaction() as tx:
            assert tx.is_interactive is True
        await SurrealDBConnectionManager.close_connection()

    @pytest.mark.asyncio
    async def test_http_uses_buffered_and_commits(self) -> None:
        _connect_http()
        client = await SurrealDBConnectionManager.get_client()
        await _clear(client)
        async with SurrealDBConnectionManager.transaction() as tx:
            assert tx.is_interactive is False
            await TxUser(id="http1", name="H").save(tx=tx)
        rows = await client.query("SELECT * FROM TxUser;", {})
        assert len(rows) == 1
        await SurrealDBConnectionManager.close_connection()

    @pytest.mark.asyncio
    async def test_ws_interactive_commit_and_rollback(self) -> None:
        _connect()
        client = await SurrealDBConnectionManager.get_client()
        await _clear(client)
        async with SurrealDBConnectionManager.transaction() as tx:
            await TxUser(id="ws1", name="W").save(tx=tx)
        rows = await client.query("SELECT * FROM TxUser;", {})
        assert len(rows) == 1
        await _clear(client)
        with pytest.raises(RuntimeError):
            async with SurrealDBConnectionManager.transaction() as tx:
                await TxUser(id="ws2", name="W2").save(tx=tx)
                raise RuntimeError("boom")
        rows = await client.query("SELECT * FROM TxUser;", {})
        assert len(rows) == 0  # CANCEL effectif
        await SurrealDBConnectionManager.close_connection()
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest tests/test_transactions.py::TestTransactionDispatchE2E -v`
Expected: FAIL (dispatch absent ; `tx.is_interactive` n'existe pas sur l'objet renvoyé / type incorrect)

- [ ] **Step 3: Recâbler `transaction()`**

Dans `connection_manager.py`, modifier l'import (ligne 7) :

```python
from .transaction import BufferedTransaction, InteractiveTransaction, Transaction
```

Remplacer toute la méthode `transaction()` (le bloc `@contextlib.asynccontextmanager async def transaction`) par :

```python
    @classmethod
    @contextlib.asynccontextmanager
    async def transaction(cls) -> Any:
        """Atomic transaction. Strategy depends on the configured connection:

        - WebSocket (``ws://`` / ``wss://``) → ``InteractiveTransaction`` on a
          dedicated connection: operations run immediately inside a server-side
          BEGIN…COMMIT; reads inside the transaction see uncommitted writes.
        - HTTP (``http://`` / ``https://``) → ``BufferedTransaction``: writes are
          buffered and flushed as one BEGIN…COMMIT at commit (v0.8.0 behavior);
          reads inside the transaction raise (HTTP is stateless).

        Exception semantics: an exception in the body triggers ``cancel()`` and
        re-raises (rollback). A server-side rollback surfaces as ``SurrealDbError``
        from ``commit()`` BEFORE deferred ``post_*`` signals fire. An exception from a
        deferred handler surfaces AFTER a durable commit (not a rollback).

        SurrealDB has no savepoints/nested transactions; nested ``transaction()``
        calls open INDEPENDENT transactions, not savepoints.

        Example::

            async with SurrealDBConnectionManager.transaction() as tx:
                users = await User.objects(tx=tx).filter(status="active").exec()
                await User.objects(tx=tx).filter(role="guest").bulk_update(role="member")
        """
        url = cls.__url or ""
        tx: Transaction
        dedicated_client: Any = None
        if url.startswith(("ws://", "wss://")):
            dedicated_client = await cls._open_client()
            tx = InteractiveTransaction(dedicated_client)
        else:
            tx = BufferedTransaction(await cls.get_client())

        try:
            await tx.begin()
            yield tx
        except Exception:
            await tx.cancel()
            raise
        else:
            await tx.commit()
            await tx.fire_post_commit()
        finally:
            if dedicated_client is not None:
                with contextlib.suppress(Exception):
                    await dedicated_client.close()
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest tests/test_transactions.py::TestTransactionDispatchE2E tests/test_transactions.py::TestTransactionE2E -v`
Expected: PASS (sauf cas devenus obsolètes traités en Task 6 — voir note)

> Note : `TestTransactionE2E::test_save_in_tx_without_id_raises` est désormais FAUX en WS (auto-id supporté). Il est corrigé en Task 6. Si exécuté ici, il échoue : c'est attendu, il sera ré-aiguillé.

- [ ] **Step 5: Lint + commit**

```bash
uv run ruff format src/ && uv run ruff check src/
git add src/surreal_orm_lite/connection_manager.py tests/test_transactions.py
git commit -m "feat(tx): dispatch transaction() to interactive (WS) or buffered (HTTP)"
```

---

## Task 6: `model_base` — `objects(tx=)`, branches `tx=` via `add()`, renforcements WS

**Files:**
- Modify: `src/surreal_orm_lite/model_base.py` (`_do_save`, `save`, `update`, `merge`, `delete`, `refresh`, `objects`)
- Test: `tests/test_transactions.py` (corriger les obsolètes + nouveaux WS)

- [ ] **Step 1: Corriger les tests obsolètes + ajouter les renforcements WS**

Dans `tests/test_transactions.py` :

1. Remplacer `test_save_in_tx_without_id_raises` par une version WS (auto-id OK) + HTTP (lève) :

```python
class TestSaveTxAutoId:
    @pytest.mark.asyncio
    async def test_ws_save_autoid_assigns_id(self) -> None:
        _connect()
        client = await SurrealDBConnectionManager.get_client()
        await _clear(client)
        u = TxUser(name="AutoWS")  # pas d'id
        async with SurrealDBConnectionManager.transaction() as tx:
            await u.save(tx=tx)
        assert u.id is not None  # id auto attribué
        await SurrealDBConnectionManager.close_connection()

    @pytest.mark.asyncio
    async def test_http_save_without_id_raises(self) -> None:
        _connect_http()
        with pytest.raises(SurrealDbError, match="explicit id"):
            async with SurrealDBConnectionManager.transaction() as tx:
                await TxUser(name="NoId").save(tx=tx)
        await SurrealDBConnectionManager.close_connection()
```

2. Remplacer `test_refresh_with_tx_raises` par :

```python
@pytest.mark.asyncio
async def test_refresh_tx_works_in_ws() -> None:
    _connect()
    client = await SurrealDBConnectionManager.get_client()
    await _clear(client)
    await TxUser(id="ref1", name="Before").save()
    await client.query("UPDATE TxUser:ref1 SET name = 'After';", {})
    u = TxUser(id="ref1", name="Before")
    async with SurrealDBConnectionManager.transaction() as tx:
        await u.refresh(tx=tx)
    assert u.name == "After"
    await SurrealDBConnectionManager.close_connection()


@pytest.mark.asyncio
async def test_refresh_tx_raises_in_http() -> None:
    _connect_http()
    u = TxUser(id="x", name="x")
    async with SurrealDBConnectionManager.transaction() as tx:
        with pytest.raises(SurrealDbError, match="WebSocket"):
            await u.refresh(tx=tx)
    await SurrealDBConnectionManager.close_connection()
```

3. Ajouter un test « lecture voit ses propres writes » (cœur de la feature) :

```python
@pytest.mark.asyncio
async def test_ws_read_sees_uncommitted_write() -> None:
    _connect()
    client = await SurrealDBConnectionManager.get_client()
    await _clear(client)
    async with SurrealDBConnectionManager.transaction() as tx:
        await TxUser(id="u1", name="InTx").save(tx=tx)
        # lecture DANS la tx → voit l'écriture non-committée
        found = await TxUser.objects(tx=tx).filter(name="InTx").exec()
        assert len(found) == 1
    await SurrealDBConnectionManager.close_connection()
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest tests/test_transactions.py -k "AutoId or refresh_tx or read_sees_uncommitted" -v`
Expected: FAIL (auto-id non supporté ; `refresh(tx=)` lève toujours ; `objects(tx=)` n'accepte pas l'argument)

- [ ] **Step 3: Modifier `model_base.py`**

3a. Remplacer la branche `tx` de `_do_save` (lignes ~174-178) par :

```python
        if tx is not None:
            if record_id is not None:
                await tx.add(f"CREATE {record_id} CONTENT $data;", {"data": data})
                return self, True
            if not tx.is_interactive:
                raise SurrealDbError(
                    "save(tx=...) requires an explicit id on a buffered (HTTP) transaction "
                    "(auto-id requires a WebSocket transaction)."
                )
            rows = await tx.add(f"CREATE {table} CONTENT $data;", {"data": data})
            record = rows[0] if isinstance(rows, list) and rows else rows
            if isinstance(record, dict):
                for key, value in record.items():
                    if hasattr(self, key):
                        object.__setattr__(self, key, value)
            return self, True
```

3b. Dans `update`, branche `tx` (lignes ~270-278), ajouter `await` devant les deux `tx.add(...)` :

```python
        if tx is not None:
            if not has_signals:
                await tx.add(f"UPDATE {record_id} CONTENT $data;", {"data": data})
                return None
            update_fields = list(data.keys())
            await pre_update.send(sender, instance=self, update_fields=update_fields)
            await tx.add(f"UPDATE {record_id} CONTENT $data;", {"data": data})
            tx.enqueue_post_commit(lambda: post_update.send(sender, instance=self, update_fields=update_fields))
            return None
```

3c. Remplacer la branche `tx` de `merge` (lignes ~324-340) par :

```python
        if tx is not None:
            update_fields = list(data_set.keys())
            if has_signals:
                await pre_update.send(sender, instance=self, update_fields=update_fields)
            await tx.add(f"UPDATE {record_id} MERGE $data;", {"data": data_set})
            if tx.is_interactive:
                # Real refresh from the tx connection (sees its own write).
                await self.refresh(tx=tx)
            else:
                # Buffered: no reads; apply merged fields to the instance directly.
                for key, value in data_set.items():
                    if hasattr(self, key):
                        object.__setattr__(self, key, value)
            if has_signals:
                tx.enqueue_post_commit(lambda: post_update.send(sender, instance=self, update_fields=update_fields))
            return None
```

3d. Dans `delete`, branche `tx` (lignes ~376-383), ajouter `await` devant les `tx.add(...)` :

```python
        if tx is not None:
            if not has_signals:
                await tx.add(f"DELETE {record_id};", None)
                return None
            await pre_delete.send(sender, instance=self)
            await tx.add(f"DELETE {record_id};", None)
            tx.enqueue_post_commit(lambda: post_delete.send(sender, instance=self))
            return None
```

3e. Remplacer la branche `tx` en tête de `refresh` (lignes ~140-141) par :

```python
        if tx is not None:
            if not tx.is_interactive:
                raise SurrealDbError(
                    "refresh() is a read; it requires a WebSocket (interactive) transaction."
                )
            if not self.get_id():
                raise SurrealDbError("Can't refresh data, not recorded yet.")
            rows = await tx.run_read("SELECT * FROM $rid;", {"rid": self._record_id()})
            record = rows[0] if isinstance(rows, list) and rows else rows
            if not isinstance(record, dict):
                raise SurrealDbError("Can't refresh data, no record found.")
            for key, value in record.items():
                if hasattr(self, key):
                    object.__setattr__(self, key, value)
            return
```

3f. Remplacer `objects` (lignes ~650-657) par :

```python
    @classmethod
    def objects(cls, tx: Transaction | None = None) -> Any:
        """
        Return a QuerySet for the model class.

        When ``tx`` is provided, the QuerySet's reads and bulk operations participate
        in that transaction.
        """
        from .query_set import QuerySet

        qs = QuerySet(cls)
        if tx is not None:
            qs._tx = tx
        return qs
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest tests/test_transactions.py -k "AutoId or refresh_tx or read_sees_uncommitted or TestTransactionE2E or TestTransactionSignalsE2E or TestMergeTxSync" -v`
Expected: PASS

- [ ] **Step 5: Lint + commit**

```bash
uv run ruff format src/ && uv run ruff check src/
git add src/surreal_orm_lite/model_base.py tests/test_transactions.py
git commit -m "feat(tx): objects(tx=), interactive tx= paths, WS auto-id/refresh/merge"
```

---

## Task 7: `query_set` — routage lectures (`run_read`) et bulk (`add`)

**Files:**
- Modify: `src/surreal_orm_lite/query_set.py` (`__init__`, `_execute_query`, `get`, `all`, `bulk_update`, `bulk_delete`, `bulk_create`)
- Test: `tests/test_transactions.py` (E2E QuerySet sous tx)

- [ ] **Step 1: Écrire les tests E2E QuerySet sous tx**

Ajouter à `tests/test_transactions.py` :

```python
class TestQuerySetTxE2E:
    @pytest.mark.asyncio
    async def test_ws_bulk_update_in_tx_commits(self) -> None:
        _connect()
        client = await SurrealDBConnectionManager.get_client()
        await _clear(client)
        await TxUser(id="a", name="guest").save()
        await TxUser(id="b", name="guest").save()
        async with SurrealDBConnectionManager.transaction() as tx:
            n = await TxUser.objects(tx=tx).filter(name="guest").bulk_update(name="member")
            assert n == 2  # interactif : rows mis à jour
        rows = await client.query("SELECT name FROM TxUser;", {})
        assert all(r["name"] == "member" for r in rows)
        await SurrealDBConnectionManager.close_connection()

    @pytest.mark.asyncio
    async def test_ws_bulk_delete_rolls_back_on_exception(self) -> None:
        _connect()
        client = await SurrealDBConnectionManager.get_client()
        await _clear(client)
        await TxUser(id="d1", name="x").save()
        with pytest.raises(RuntimeError):
            async with SurrealDBConnectionManager.transaction() as tx:
                await TxUser.objects(tx=tx).filter(name="x").bulk_delete()
                raise RuntimeError("rollback")
        rows = await client.query("SELECT * FROM TxUser;", {})
        assert len(rows) == 1  # delete annulé
        await SurrealDBConnectionManager.close_connection()

    @pytest.mark.asyncio
    async def test_http_bulk_update_in_tx_commits(self) -> None:
        _connect_http()
        client = await SurrealDBConnectionManager.get_client()
        await _clear(client)
        await TxUser(id="h1", name="guest").save()
        async with SurrealDBConnectionManager.transaction() as tx:
            await TxUser.objects(tx=tx).filter(name="guest").bulk_update(name="member")
        rows = await client.query("SELECT name FROM TxUser;", {})
        assert rows[0]["name"] == "member"
        await SurrealDBConnectionManager.close_connection()

    @pytest.mark.asyncio
    async def test_http_read_in_tx_raises(self) -> None:
        _connect_http()
        client = await SurrealDBConnectionManager.get_client()
        await _clear(client)
        async with SurrealDBConnectionManager.transaction() as tx:
            with pytest.raises(SurrealDbError, match="WebSocket"):
                await TxUser.objects(tx=tx).filter(name="x").exec()
        await SurrealDBConnectionManager.close_connection()
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest tests/test_transactions.py::TestQuerySetTxE2E -v`
Expected: FAIL (`QuerySet` n'a pas `_tx` ; bulk/read non routés)

- [ ] **Step 3: Modifier `query_set.py`**

3a. Dans `__init__` (après `self._annotations = {}`, ligne ~53), ajouter :

```python
        self._tx: Any = None
```

3b. Remplacer `_execute_query` (lignes ~339-359) par :

```python
    async def _execute_query(self, query: str, variables: dict[str, Any] | None = None) -> list[dict[str, Any]]:
        """
        Execute a read query. Routes through the transaction when one is attached
        (``objects(tx=)``), so reads participate in the transaction and see its
        uncommitted writes (WS). On a buffered (HTTP) tx, ``run_read`` raises.
        """
        vars_ = variables if variables is not None else self._variables
        compiled = remove_quotes_for_variables(query)
        if self._tx is not None:
            try:
                return await self._tx.run_read(compiled, vars_)  # type: ignore[no-any-return]
            except NotFoundError:
                return []
        client = await SurrealDBConnectionManager.get_client()
        try:
            return await client.query(compiled, vars_)  # type: ignore
        except NotFoundError:
            return []
```

3c. Dans `get`, remplacer la branche `if id_item:` (lignes ~411-433) par une version qui route sous tx :

```python
        if id_item:
            if isinstance(id_item, RecordID):
                record_id = id_item
            else:
                raw = str(id_item)
                if raw.startswith("`") and raw.endswith("`"):
                    raw = raw[1:-1]
                record_id = RecordID(self._model_table, raw)

            if self._tx is not None:
                rows = await self._tx.run_read("SELECT * FROM $rid;", {"rid": record_id})
                data = rows[0] if isinstance(rows, list) and rows else rows
                if not data:
                    raise SurrealDbNotFoundError("No result found.")
                return self.model.from_db(data)

            client = await SurrealDBConnectionManager.get_client()
            try:
                data = await client.select(record_id)
            except NotFoundError:
                raise SurrealDbNotFoundError("No result found.") from None
            if isinstance(data, list):
                if len(data) == 0:
                    raise SurrealDbNotFoundError("No result found.")
                data = data[0]
            if data is None:
                raise SurrealDbNotFoundError("No result found.")
            return self.model.from_db(data)
```

3d. Remplacer `all` (lignes ~443-457) par :

```python
    async def all(self) -> Any:
        """
        Fetch all records from the associated table.
        """
        if self._tx is not None:
            try:
                rows = await self._tx.run_read(f"SELECT * FROM {self._model_table};", {})
            except NotFoundError:
                return self.model.from_db([])
            return self.model.from_db(rows if isinstance(rows, list) else [])

        client = await SurrealDBConnectionManager.get_client()
        try:
            results = await client.select(self._model_table)
        except NotFoundError:
            return self.model.from_db([])
        return self.model.from_db(results)
```

3e. Remplacer la fin de `bulk_update` (lignes ~706-711, le bloc `all_vars`/`_execute_query`/return) par :

```python
        all_vars = {**self._variables, **where_vars, **set_vars}
        if self._tx is not None:
            rows = await self._tx.add(query, all_vars)
            return len(rows) if isinstance(rows, list) else 0
        results = await self._execute_query(query, all_vars)
        if isinstance(results, list):
            return len(results)
        return 0
```

3f. Remplacer la fin de `bulk_delete` (lignes ~724-732) par :

```python
        where_clause, where_vars = self._build_where()
        query = f"DELETE {self._model_table}{where_clause} RETURN BEFORE;"
        all_vars = {**self._variables, **where_vars}
        if self._tx is not None:
            rows = await self._tx.add(query, all_vars)
            return len(rows) if isinstance(rows, list) else 0
        results = await self._execute_query(query, all_vars)
        if isinstance(results, list):
            return len(results)
        return 0
```

3g. Dans `bulk_create`, remplacer le bloc final (lignes ~669-674, `client = ...` jusqu'au `return []`) par :

```python
        if self._tx is not None:
            rows = await self._tx.add(f"INSERT INTO {self._model_table} $data;", {"data": data_list})
            if self._tx.is_interactive and isinstance(rows, list):
                return self.model.from_db(rows)  # type: ignore
            return models  # buffered: not executed until commit; return inputs

        client = await SurrealDBConnectionManager.get_client()
        results = await client.insert(self._model_table, data_list)

        if isinstance(results, list):
            return self.model.from_db(results)  # type: ignore
        return []
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest tests/test_transactions.py::TestQuerySetTxE2E -v`
Expected: PASS

- [ ] **Step 5: Lint + commit**

```bash
uv run ruff format src/ && uv run ruff check src/
git add src/surreal_orm_lite/query_set.py tests/test_transactions.py
git commit -m "feat(tx): QuerySet reads/bulk participate in transactions (objects(tx=))"
```

---

## Task 8: Exports, version bump et documentation

**Files:**
- Modify: `src/surreal_orm_lite/__init__.py`
- Modify: `pyproject.toml`
- Modify: `docs/ROADMAP.md`
- Test: `tests/test_transactions.py` (export)

- [ ] **Step 1: Écrire le test d'export + version**

Ajouter à `tests/test_transactions.py` :

```python
def test_tx_subclasses_exported() -> None:
    import surreal_orm_lite

    assert surreal_orm_lite.BufferedTransaction is BufferedTransaction
    assert surreal_orm_lite.InteractiveTransaction is InteractiveTransaction
    assert surreal_orm_lite.__version__ == "0.9.0"
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `uv run pytest tests/test_transactions.py::test_tx_subclasses_exported -v`
Expected: FAIL (`AttributeError` / version `0.8.0`)

- [ ] **Step 3: Mettre à jour les exports et la version**

3a. Dans `src/surreal_orm_lite/__init__.py`, ligne 1 :

```python
__version__ = "0.9.0"
```

3b. Remplacer la ligne `from .transaction import Transaction` par :

```python
from .transaction import BufferedTransaction, InteractiveTransaction, Transaction
```

3c. Dans `__all__`, après `"Transaction",` ajouter :

```python
    "BufferedTransaction",
    "InteractiveTransaction",
```

3d. Dans `pyproject.toml`, mettre `version = "0.9.0"` (champ `[project] version`).

3e. Dans `docs/ROADMAP.md`, ligne 201 (tableau Phase A), passer la ligne v0.9.0 à l'état fait :

```markdown
| ✅ v0.9.0 | Transactions ORM (QuerySet): `objects(tx=)`, bulk under `tx`, no savepoints (SurrealDB) | idem                      |
```

Et ligne 114 (tableau comparatif), remplacer la cellule Transactions par :

```markdown
| Transactions (`tx=`)          | yes        | ✅ v0.8 core, v0.9 QuerySet |
```

3f. Dans `docs/ROADMAP.md`, sous la section « Completed versions », ajouter après le bloc v0.8.0 :

```markdown
### Version 0.9.0 — Transactions ORM (QuerySet)

- `objects(tx=)`: QuerySet reads, bulk_update/bulk_delete/bulk_create participate in a transaction
- WebSocket interactive transactions (dedicated connection): reads see uncommitted writes,
  auto-id `save(tx=)`, working `refresh(tx=)`
- HTTP keeps the v0.8.0 buffered model (writes batched; reads in tx raise — WS required)
- Savepoints / nested transactions: NOT supported (SurrealDB only has flat BEGIN/COMMIT/CANCEL);
  nested `transaction()` opens independent transactions
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `uv run pytest tests/test_transactions.py::test_tx_subclasses_exported -v`
Expected: PASS

- [ ] **Step 5: Lint + commit**

```bash
uv run ruff format src/ && uv run ruff check src/
git add src/surreal_orm_lite/__init__.py pyproject.toml docs/ROADMAP.md tests/test_transactions.py
git commit -m "feat(tx): export tx subclasses, bump to 0.9.0, update ROADMAP"
```

---

## Task 9: Vérification finale (suite complète, mypy, couverture, 2 serveurs)

**Files:** aucun changement de code (corrections si échec)

- [ ] **Step 1: Suite complète + couverture sur SurrealDB 2.6.0**

Run:
```bash
SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest tests/ -v --cov=src.surreal_orm_lite
```
Expected: tout PASS, couverture ≥ 70 %.

- [ ] **Step 2: Lint + types**

Run:
```bash
uv run ruff check src/ && uv run ruff format --check src/ && uv run mypy src/
```
Expected: aucun problème.

- [ ] **Step 3: Rejouer la suite sur SurrealDB 3.1.3**

Run:
```bash
docker rm -f surreal
docker run -d --name surreal -p 8000:8000 surrealdb/surrealdb:v3.1.3 start --user root --pass root
# attendre health 200
SURREALDB_HOST=localhost SURREALDB_PORT=8000 uv run pytest tests/ -v
```
Expected: tout PASS sur 3.1.3 aussi. Nettoyage : `docker rm -f surreal`.

- [ ] **Step 4: Commit final (si corrections)**

```bash
git add -A
git commit -m "test(tx): verify v0.9.0 green on SurrealDB 2.6.0 and 3.1.3"
```

---

## Self-Review (couverture du spec)

- Spike WS (spec §Pré-requis) → **Task 1**.
- Connexion dédiée + `_open_client` (spec §Architecture/connection_manager) → **Task 2, 5**.
- Deux stratégies + interface commune (spec §Architecture) → **Task 3, 4**.
- Dispatch HTTP/WS (spec §connection_manager) → **Task 5**.
- `objects(tx=)`, branches `tx=`, renforcements WS (spec §model_base) → **Task 6**.
- Routage lectures + bulk (spec §query_set) → **Task 7**.
- Savepoints documentés non disponibles (spec §Savepoints) → **Task 8 (docstring + ROADMAP)**.
- Matrice de capacités (spec) → couverte par tests Task 6/7 (WS lit, HTTP lève).
- Compat ascendante + tests v0.8.0 réécrits (spec §Compatibilité) → **Task 3, 6**.
- Garanties transverses (couverture/lint/mypy/2 serveurs) → **Task 9**.
- Version + exports (spec §Fichiers touchés) → **Task 8**.
